# Input student marks
marks = float(input("Enter your marks (out of 100): "))

# Predict grade
if marks >= 90:
    grade = 'A+'
elif marks >= 80:
    grade = 'A'
elif marks >= 70:
    grade = 'B+'
elif marks >= 60:
    grade = 'B'
elif marks >= 50:
    grade = 'C'
else:
    grade = 'F'

print("Your grade is: ",grade)
